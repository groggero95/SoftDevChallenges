egrep -i "(failed|accepted) password" | sed 's/.*: Failed/f/' |  sed 's/.*: Accepted/a/' | sed -r 's_^([af]).* ([0-9]+\.[0-9]+\.[0-9]+\.[0-9]+).*_\2 \1  _'| sort -n | uniq -c | awk 'BEGIN {FS=" "} {if((ip==$2) && ($1 > count)) {print ip, $1, count} else {ip=$2; action=$3; count=$1}}'
